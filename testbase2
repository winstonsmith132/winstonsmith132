/**
 * The event handler triggered when opening the spreadsheet.
 * @param {Event} e The onOpen event.
 */
function onOpen(e) {
// Add a custom menu to the spreadsheet.
if
[false if w_0] true[ if b_1
      .addItem('First item', 'menuItem1')
      .addToUi();
}
